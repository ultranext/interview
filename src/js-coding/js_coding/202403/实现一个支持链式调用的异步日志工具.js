/**
 * 实现一个支持链式调用的异步日志打印工具
 * 
 * class Logger {
  constructor() {
    //
  }
 
  log(msg) {
    //
  }
 
  delay(time) {
    //
  }
}

const logger = new Logger();

logger.log(1).delay(1000).log(2).log(3).delay(500).log(4);

setTimeout(() => {
  logger.log(5).delay(500).log(6);
}, 1000);
 */

